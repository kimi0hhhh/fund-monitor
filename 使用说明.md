# 基金监控小工具 · 使用说明

## 文件位置
- **可执行程序**：`dist/基金监控.exe`（单文件，双击即可运行，带专属图标）
- 源代码：`fund_monitor.py`、`fund_analysis.py`，图标源文件：`make_icon.py` / `app.ico`

## ⚡ 第一次使用前先配置 LLM（仅分析模式需要）
1. 打开程序 → 顶部点「**分析**」tab → 右上角「**⚙ 设置**」
2. 填入你的 **DeepSeek API key**（https://platform.deepseek.com 申请，最便宜的够用）
3. 保存即可。默认配置：model=`deepseek-v4-pro`，base_url=`https://api.deepseek.com`（想省成本可改 `deepseek-v4-flash`）
4. 持仓功能完全免费，不依赖 LLM

## 分析模式（投研团队流水线）
顶部点「**分析**」切到分析视图，含 3 个子 tab：

### ① 今日分析
- 选择基金 → 点「🤖 开始分析」
- 后台依次执行：拉取 90 天历史净值 → 计算 MA/RSI/回撤/波动率 → 让 AI 扮演完整投研团队（技术分析师/基本面/新闻/情绪 → 多空研究员 → 主管 → 交易员 → 风控三方 + 风险主管）→ 输出结构化报告
- 报告包含：核心结论、今日分析（趋势/关键价位/动量/风险）、明日预测（方向/幅度/信心）、加减仓建议（操作/仓位/入场/目标/止损）、关键风险与催化、技术指标摘要
- 通常 30-60 秒/只基金（取决于 API 速度）

### ② 历史预测
- 列出所有分析过的日期，点击查看当日预测详情
- 每天的分析预测自动存到 `analysis_history.json`

### ③ 复盘
- 选择历史预测日期 + 基金 + 填入今日**实际**涨跌幅
- 后台自动对比昨日预测 vs 今日实际，给出：
  - 方向是否正确（UP/DOWN/FLAT 对比）
  - 幅度偏差
  - 综合准确率（0-100%，方向对 + 误差小 = 高分）
  - AI 分析偏差原因（让 AI 解释为什么预测偏了）

## 持仓监控（主窗口默认 tab）
1. **录入持仓**：输入 6 位基金代码 + 持有金额 + 确认规则，按当前净值直接折算成份额
2. **买入（加仓）**：选中一行或输入代码后点"买入"，输入金额，按该基金 T+N 规则自动确认
3. **卖出（减仓）**：选中一行后点"卖出"，输入金额（或输入"全部"清仓），同样按 T+N 规则确认
4. **改规则**：选中一行后点"改规则"，设置该基金确认天数（0/1/2）
5. **撤单**：提交后未确认前，可在下方"待确认交易"列表点"撤单"
6. **删除基金**：选中一行后点"删除"
7. **悬浮窗**：右上角点"悬浮窗"→ 弹出一个独立的小窗（无边框、置顶、可随意拖动到屏幕任意位置），精简显示总资产/今日收益/各基金涨跌；点"展开完整版"回到主窗口
8. **数据展示**：名称、估算涨幅、持有金额、今日收益、累计收益、持有份额、估算净值
9. **顶部汇总**：总资产、今日估算收益、今日收益率、累计收益
10. **自动刷新**：每 30 秒自动更新，也可点"立即刷新"

## 基金交易规则（T+N，每只基金可单独设置）
- 不同基金确认规则不同，可在录入时选择，或选中后点"改规则"修改：
  - **T+0**（0）：当天确认（货币基金快赎等）
  - **T+1**（1，默认）：大多数股票/混合/债券基金
  - **T+2**（2）：QDII 等跨境基金
- **买入**：交易日 **15:00 前**提交 → 按**当天**净值折算份额，**T+N** 天后确认到账；
  **15:00 后**或周末提交 → 顺延到**下一个交易日**的净值
- **卖出**：同样的 15:00 截止规则，按确认净值日结算金额，T+N 天后到账生效
- 待确认的交易会显示在底部"待确认交易"列表（含净值日和到账日），确认后自动消失
- 说明：交易日历只区分工作日/周末，法定节假日不单独判断（会随净值日期自然顺延）

## 颜色约定（A股习惯）
- 红色 = 涨，绿色 = 跌

## 数据说明
- 交易日内显示**盘中实时估值**（数据来源：腾讯财经 + 蛋卷基金）
- 休市 / 净值更新后显示**最新官方净值的日涨跌**
- "今日收益"按 市值 × 涨幅 估算，仅供参考，实际以基金公司结算为准
- 累计收益 = 当前市值 + 已赎回金额 - 总投入金额

## 数据保存
- `funds_data.json`：基金列表、持仓份额、交易记录
- `analysis_config.json`：LLM API key、模型（仅本机，不上传）
- `analysis_history.json`：每日预测记录（用于复盘）
- 三个文件都在 exe 同目录，换电脑时全部拷贝即可

## 常见问题
- **窗口打不开/白屏**：程序依赖 WebView2 运行时（Windows 11 和绝大多数 Win10 已自带），若缺失可到微软官网下载 "WebView2 Runtime" 安装
- **添加后显示"未找到"**：请确认是 6 位基金代码（如 110022），股票代码不支持
- **估值不更新**：非交易时间（交易日 9:30-15:00）没有盘中估值，显示的是最新净值涨跌
- **杀毒软件报毒**：PyInstaller 打包的单文件 exe 偶尔会误报，添加信任即可
- **买入后长时间没确认**：请确认你在 15:00 前买入且当天是交易日；若跨了法定节假日，确认会顺延到节后首个交易日
- **分析模式报"请配置 API key"**：点分析 tab → 右上角 ⚙ 设置 → 填入 DeepSeek key → 保存
- **分析模式调用失败**：检查 key 是否正确、余额是否充足、网络是否能访问 api.deepseek.com
